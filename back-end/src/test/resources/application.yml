spring:
  datasource:
    url: jdbc:tc:postgresql:14-alpine:///spring-blog

  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  jpa:
    hibernate:
      ddl-auto: update

server:
  servlet:
    context-path: /api
