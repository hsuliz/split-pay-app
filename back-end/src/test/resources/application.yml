spring:
  datasource:
    driver-class-name=org:
      postgresql:

    url: jdbc:tc:postgresql:14-alpine:///spring-blog

    jpa:
      hibernate:
        ddl-auto:update
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

server:
  servlet:
    context-path: /api